export const ROUTES = {
  landing:       "/",
  layoutSelect:  "/layout-select",
  modeSelect:    "/mode-select",
  instructions:  "/instructions",
  capture:       "/capture",
  upload:        "/upload",
  select:        "/select",
  edit:          "/edit",
  view:          "/view",
} as const;
